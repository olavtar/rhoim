# Optional HPA; tune as needed
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: rhoim-gateway
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rhoim-gateway
  minReplicas: 1
  maxReplicas: 5
  metrics: []
